#include<stdio.h>
main()
{
	int rev,r,n,i,t;
	printf("Enter the number: ");
	scanf("%d",&n);
	t=n;
	while(i<n)
	{
		r=n%10;
		rev=rev*10+r;
		n=n/10;
	}
	if(rev==t)
	{
		printf("It is a palindrome");
	}
	else
	{
		printf("It is not a palindrome");
	}
}
