//x to the power y
#include<stdio.h>
main()
{
	int x,y,i=1,pow=1;
	printf("Enter the base and power value: ");
	scanf("%d%d",&x,&y);
	while(i<=y)
	{
		pow*=x;
		i++;
	}
	printf("%d ^ %d = %d",x,y,pow);
}
